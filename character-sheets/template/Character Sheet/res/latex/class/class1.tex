
\def \${class1}level{${class1_level}}

% --- *Class1 ---

% --- Hit Dice
\def \${class1}hitdice{\${class1}level d${class1_hit_die}}

% --- Hit Points
\def \${class1}hitpoints{${class1_hit_points}}

% --- Base Attack
\FPeval{\${class1}baseattack}{trunc(${class1_bab}, 0)}

% --- Saving Throws
\FPeval{\${class1}basefortitude}{trunc(${class1_fort}, 0)}
\FPeval{\${class1}basereflex}{trunc(${class1_ref}, 0)}
\FPeval{\${class1}basewill}{trunc(${class1_will}, 0)}

% --- *Class1 ---

% --- Skills
